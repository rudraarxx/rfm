# Plan: Decommission MongoDB (01-01)

**Phase:** 01-architecture-purge
**Wave:** 1
**Depends on:** None
**Requirements:** [ARCH-01, ARCH-03]
**Status:** Ready to execute

## Summary
Remove all MongoDB and Mongoose dependencies from the project. This includes deleting connection logic, models, and maintenance code that interacts with the database.

## Tasks

<task id="01-01-01" wave="1">
  <action>Remove `mongoose` dependency from `package.json`.</action>
  <read_first>
    - [package.json](file:///Users/rupesh_sontakke/personal_dev_code/RFM/package.json)
  </read_first>
  <acceptance_criteria>
    - `package.json` does not contain the "mongoose" key in dependencies.
    - `npm list mongoose` returns an error or empty result.
  </acceptance_criteria>
</task>

<task id="01-01-02" wave="1">
  <action>Delete the MongoDB connection and model files.</action>
  <read_first>
    - [src/lib/mongodb.ts](file:///Users/rupesh_sontakke/personal_dev_code/RFM/src/lib/mongodb.ts)
    - [src/models/Station.ts](file:///Users/rupesh_sontakke/personal_dev_code/RFM/src/models/Station.ts)
  </read_first>
  <acceptance_criteria>
    - `src/lib/mongodb.ts` is deleted.
    - `src/models/Station.ts` is deleted.
  </acceptance_criteria>
</task>

<task id="01-01-03" wave="1">
  <action>Refactor `scrape-and-upload-stations.mjs` to remove MongoDB upload logic.</action>
  <read_first>
    - [scripts/scrape-and-upload-stations.mjs](file:///Users/rupesh_sontakke/personal_dev_code/RFM/scripts/scrape-and-upload-stations.mjs)
  </read_first>
  <acceptance_criteria>
    - `scripts/scrape-and-upload-stations.mjs` no longer imports `mongoose`.
    - The `MONGODB_URI` check and the subsequent upload block (approx lines 408-467) are removed.
    - The script successfully runs and saves the local JSON.
  </acceptance_criteria>
</task>

## Verification
- Run `npm install` to update the lockfile.
- Run `node scripts/scrape-and-upload-stations.mjs` and ensure it completes without DB errors.
