# Plan: Implement ETag Sync & Memory API (01-02)

**Phase:** 01-architecture-purge
**Wave:** 2
**Depends on:** 01-01
**Requirements:** [ARCH-02, ARCH-04]
**Status:** Ready to execute

## Summary
Refactor the stations API to serve data from the local JSON cache with in-memory filtering. Implement ETag-based conditional fetching to minimize bandwidth for both Web and Mobile clients.

## Tasks

<task id="01-02-01" wave="1">
  <action>Refactor `src/app/api/stations/route.ts` for memory-based search.</action>
  <read_first>
    - [src/app/api/stations/route.ts](file:///Users/rupesh_sontakke/personal_dev_code/RFM/src/app/api/stations/route.ts)
    - [src/data/radio-browser-cache.json](file:///Users/rupesh_sontakke/personal_dev_code/RFM/src/data/radio-browser-cache.json)
  </read_first>
  <acceptance_criteria>
    - The API route reads station data from `src/data/radio-browser-cache.json` using `readFileSync` (cached at module level).
    - Search filtering (state, city, keyword) is implemented using JS array methods.
    - Full-text search is replaced by basic case-insensitive substring matching on name/tags.
  </acceptance_criteria>
</task>

<task id="01-02-02" wave="1">
  <action>Implement ETag generation and 304 response in API route.</action>
  <read_first>
    - [src/app/api/stations/route.ts](file:///Users/rupesh_sontakke/personal_dev_code/RFM/src/app/api/stations/route.ts)
  </read_first>
  <acceptance_criteria>
    - The API response includes a unique `ETag` header (MD5 hash of the data).
    - If the `If-None-Match` request header matches the ETag, the API returns `304 Not Modified`.
  </acceptance_criteria>
</task>

<task id="01-02-03" wave="2">
  <action>Implement conditional fetching in Flutter client.</action>
  <read_first>
    - [rfm_mobile/lib/services/radio_service.dart](file:///Users/rupesh_sontakke/personal_dev_code/RFM/rfm_mobile/lib/services/radio_service.dart) (Assumed path, needs verification)
  </read_first>
  <acceptance_criteria>
    - The Flutter app sends the `If-None-Match` header in its station fetch request.
    - If a `304` is received, the app uses its local `shared_preferences` or file-cached JSON.
  </acceptance_criteria>
</task>

## Verification
- Run `curl -I http://localhost:3000/api/stations` to verify ETag header.
- Run `curl -I -H "If-None-Match: [HASH]" http://localhost:3000/api/stations` to verify 304 response.
- Verify Flutter app logs show 304 status and local cache usage.
